# CLAUDE.md

このファイルは、Claude Code (claude.ai/code) がこのリポジトリでコードを操作する際のガイダンスを提供します。

## 会話ガイドライン

- 常に日本語で会話する

## 開発哲学

### テスト駆動開発（TDD）

- 原則としてテスト駆動開発（TDD）で進める
- 期待される入出力に基づき、まずテストを作成する
- 実装コードは書かず、テストのみを用意する
- テストを実行し、失敗を確認する
- テストが正しいことを確認できた段階でコミットする
- その後、テストをパスさせる実装を進める
- 実装中はテストを変更せず、コードを修正し続ける
- すべてのテストが通過するまで繰り返す

## プロジェクト概要

これは、複数フロア（3Fと4F）にわたる社員の座席配置を管理できるNode.jsベースのオフィスレイアウト管理アプリケーションです。座席割り当て、部署ゾーン、社員データを管理するための閲覧モードと管理モードの両方を提供します。

## コマンド

### 開発用
- `npm start` - ポート9000でExpressサーバーを開始
- `node server.js` - サーバーを開始する別の方法

### Docker
- `docker build -t office-layout-app .` - Dockerイメージをビルド
- `docker run -p 9000:9000 office-layout-app` - コンテナを実行

## アーキテクチャ

### バックエンド (server.js)
- **Expressサーバー** ポート9000でCORS有効
- **データ管理** 自動バックアップシステム付き（ファイルあたり最大10バックアップ）
- **APIエンドポイント**:
  - `/api/initial-data` - 社員マスターデータのGET/POST
  - `/api/layouts/default` - バージョン管理付きレイアウトデータのGET/POST
  - `/api/download-initial-data` - マスターデータダウンロード用GET
- **バージョン管理** システムで同時編集の競合を防止
- **ファイル構造**:
  - `data/initial_data.json` - 社員マスターデータ
  - `data/layout_data.json` - レイアウト設定
  - `data/backup/` - 自動タイムスタンプバックアップ

### フロントエンド (public/)
- **シングルページアプリケーション** バニラJavaScript使用
- **ドラッグ&ドロップインターフェース** 座席割り当て用
- **マルチフロア対応** （3Fと4F）
- **デュアルモード**: 閲覧モード（読み取り専用）と管理モード（編集可能）
- **機能**:
  - 部署フィルタ付き社員リスト
  - 座席結合/分割機能
  - 部署ゾーンの色分け
  - 印刷用レイアウト（A3/A4）
  - 下書き保存/読み込み（localStorage）
  - 競合解決付きサーバー同期

### データ構造
- **社員データ**: `{ employeeData: {}, teamColors: {}, departmentColors: {} }`
- **レイアウトデータ**: `{ _version: number, layout: { floors: { floorId: { seatMap, mergedSeats, memoData, departmentZones } } } }`
- **座席グリッド**: 22島（1行11島、2行）、島あたり2席（フロアあたり合計88席）

### 主要コンポーネント
- **バックアップシステム**: データ変更前の自動タイムスタンプバックアップ
- **バージョン管理**: 同時編集によるデータ損失を防止
- **マルチフロア管理**: フロアごとの個別レイアウトデータ
- **部署ゾーン**: 組織のグループ化のための色分けセクション
- **ドラッグ&ドロップ**: 直感的な座席割り当てインターフェース

## ファイル場所
- サーバーロジック: `server.js`
- フロントエンド: `public/index.html`, `public/js/script.js`, `public/css/style.css`
- データファイル: `data/initial_data.json`, `data/layout_data.json`
- マニュアル: `public/manual.html`